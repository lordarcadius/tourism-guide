<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.vipul.imagescroll">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gujarat"></activity>
        <activity android:name=".patna" />
        <activity android:name=".chennai" />
        <activity android:name=".kerala" />
        <activity android:name=".mumbai" />
        <activity android:name=".lavasa" />
        <activity android:name=".jammu" />
        <activity android:name=".delhi" />
        <activity android:name=".ayodhya" />
        <activity android:name=".amritsar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>